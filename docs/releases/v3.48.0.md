- **The license is now Apache License 2.0.** Copies distributed from this release on are under Apache-2.0: the text is in [LICENSE](./LICENSE), and the attribution notice Apache-2.0 asks for is in the new [NOTICE](./NOTICE) file, which ships inside the npm package too. Copies you already have up to v3.47.0 stay under MIT and are not affected retroactively, so staying on the version you have needs no action from you. The bundled fluent-korean guidance keeps its own MIT terms regardless of this change.
- **The README only claims figures that can still be traced.** The 18.6% number is gone from the README and the project page, along with `docs/harness-impact.png`, the chart that existed to show it. A retracted figure keeps getting quoted as long as an image of it is still in the repository, so the chart went with the number.
- **One diagram now explains how the tool fits into a session.** `docs/how-it-works.svg` traces the order of events — session start, hook attachment, delegation — and sits near the top of the README, so you can see what runs where before reading what the statusline shows you.
- Both READMEs were rewritten in the voice the project page already uses, and the stale sentences and dead links left over from earlier edits are gone. The `profile-map --refresh` entry in `docs/ROUTE_SCAN.md` now describes what the command actually fetches instead of the refresh cycle it rides on.

---

## 한국어

- **라이선스가 Apache License 2.0 으로 바뀌었습니다.** 이 릴리스부터 배포되는 사본은 Apache-2.0 조건을 따르며, 원문은 [LICENSE](./LICENSE) 에, 출처 표기는 새로 추가한 [NOTICE](./NOTICE) 에 있습니다. NOTICE 는 npm 패키지에도 함께 담깁니다. v3.47.0 까지 받으신 사본은 계속 MIT 조건이고 그 조건은 소급해 바뀌지 않으므로, 쓰시던 버전을 그대로 두신다면 조치할 일은 없습니다. 함께 배포하는 fluent-korean 지침 원문은 이 변경과 무관하게 자체 MIT 조건을 유지합니다.
- **README 가 측정한 수치만 말합니다.** 근거를 다시 짚을 수 없게 된 18.6% 수치를 README 와 소개 페이지에서 걷어 내고, 그 수치를 보여 주던 `docs/harness-impact.png` 도 함께 지웠습니다. 수치 하나가 문서 여러 곳에 남아 있으면 철회한 뒤에도 인용이 계속되므로, 그림까지 같이 지웠습니다.
- **동작 방식을 그림 한 장으로 설명합니다.** 세션이 시작되고 훅이 붙고 위임이 일어나기까지의 순서를 `docs/how-it-works.svg` 에 그려 README 상단에 실었습니다. 도구가 statusline 에 무엇을 보여 주는지 읽기 전에, 무엇이 어디에 끼어드는지 먼저 알 수 있습니다.
- 두 README 의 문체를 소개 페이지와 맞추고, 정리 과정에서 남아 있던 옛 문장과 끊긴 링크를 걷어 냈습니다. `docs/ROUTE_SCAN.md` 의 `profile-map --refresh` 설명도 갱신 주기를 따라 적던 문장 대신 그 명령이 실제로 무엇을 가져오는지 적습니다.
